$kind: http-request
url: "{{baseUrl}}/s/nonexistent_zzz"
method: GET
scripts:
  - type: afterResponse
    code: |-
      pm.test('不存在的短链应返回 42100', function () {
          pm.expect(pm.response.json().code).to.eql(42100);
      });
    language: text/javascript
order: 2000
