$kind: http-request
url: "{{baseUrl}}/api/link/detail/99999999999999"
method: GET
headers:
  Authorization: Bearer {{token}}
scripts:
  - type: afterResponse
    code: |-
      pm.test('不存在ID应返回 42100', function () {
          pm.expect(pm.response.json().code).to.eql(42100);
      });
    language: text/javascript
order: 2000
